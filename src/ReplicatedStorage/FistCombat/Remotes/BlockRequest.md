# BlockRequest

**Class:** RemoteEvent
**Direction:** Client to server

Carries a boolean block request. The server validates it before setting the `Blocking` Attribute. `DamageService` also checks the defender's facing before reducing damage.
