# PunchRequest

**Class:** RemoteEvent
**Direction:** Client to server

The client sends the configured attack name after local playback begins. The server validates rate limits, R6 requirements, combat state, attack order, special eligibility, range, facing, targets, and damage. The client never sends damage or target data.
