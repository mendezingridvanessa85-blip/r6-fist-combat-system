# Source Mirror

This directory mirrors the runtime scripts and non-script contracts used by the Roblox Studio place. Keep it synchronized with Studio when gameplay code changes so GitHub remains a readable source reference.

RemoteEvents and the VFX template are documented as Markdown because they are Studio instances rather than standalone Luau files. This directory is not an automatic Studio importer.
